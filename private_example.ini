[google]
api_key = <insert google custom search API key here>

[network]
host = 0.0.0.0
port = 5000
restrict_ips = <list of IP addresses to allow access from, or None if no restriction desired>

[gunicorn]
num_workers = 5

